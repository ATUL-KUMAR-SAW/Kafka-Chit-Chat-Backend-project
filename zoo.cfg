# Minimal ZooKeeper configuration for containerized deployment
tickTime=2000
dataDir=/tmp/zookeeper
clientPort=2181
maxClientCnxns=60
# Disable admin server to save memory and avoid port conflicts
admin.enableServer=false
